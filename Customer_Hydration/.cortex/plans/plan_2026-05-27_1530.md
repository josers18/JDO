---
created: 2026-05-27T15:30:33.654Z
session: 2baf445c-0da5-46ee-a19d-dce4f8cb7ff9
working_directory: /Users/jsifontes/Documents/Git/JDO/Customer_Hydration
---

## Context

The JDO monorepo already has two Snowflake-native projects (`Financial_Trades_Generation`, `Snowflake_CSAT_NPS`) documented individually but with no unified Snowflake documentation hub. The new `Snowflake/` folder will serve as the **central documentation hub** for all Snowflake infrastructure, covering the shared `FINS.PUBLIC` schema, both pipelines, shared utilities (retry wrapper, daily job report, email notifications), warehouses, and cross-project architecture.\n\nExisting patterns to follow:\n- shields.io badges (`for-the-badge` style) in centered `<div>` blocks\n- Mermaid diagrams inline (erDiagram, flowchart, sequenceDiagram)\n- GFM tables for metrics, schedules, and inventories\n- SQL code blocks for quick-start / verification queries\n- Relative links between docs\n\n## Implementation Steps\n\n### 1. Create `Snowflake/README.md` — Landing page / hub\n\nContent:\n- Centered badge block (Snowflake, SQL, Snowpark Python, Data Cloud, CRON Tasks, Accounts count, Trades count)\n- Tagline: **Snowflake-native** · **Automated pipelines** · **FINS.PUBLIC**\n- **Overview** — brief description of the Snowflake layer\n- **Architecture diagram** — Mermaid `graph LR` showing DC datashare → procedures → tables → downstream (Data Cloud, CSAT, Trades)\n- **Pipelines at a Glance** table (name, cadence, procedure, target, rows)\n- **Quick Start** SQL block\n- **Documentation index** table linking to all docs/ files\n- **Related Projects** table linking to `Financial_Trades_Generation/` and `Snowflake_CSAT_NPS/`\n\n### 2. Create `Snowflake/docs/ARCHITECTURE.md` — Full architecture doc\n\nContent:\n- **ER Diagram** (Mermaid `erDiagram`) — all 7 core tables (MASTER_ACCOUNTS, CSAT_NPS_DATA, FINANCIAL_TRADES, TRADE_GENERATION_CONFIG, INSTRUMENT_UNIVERSE, TASK_EXECUTION_LOG, FINANCIAL_TRANSACTIONS)\n- **Data Flow Diagram** (Mermaid `flowchart TD`) — subgraphs: External Sources (FINSDC3_DATASHARE) → Scheduled Tasks → Stored Procedures → Tables → Views\n- **Pipeline Sequence Diagrams** (2 Mermaid `sequenceDiagram`s) — daily trade pipeline + CSAT monthly pipeline\n- **Warehouse Strategy** table (name, size, purpose, auto-suspend)\n- **Notification & Alerting** section — email integrations, daily job report flow\n- **Retry Strategy** section — SP_RETRY_WRAPPER (exponential backoff, 30s→60s, 2 retries)\n- **Key Design Decisions** table (rationale for MERGE dedup, HASH-based pseudo-randomness, retry wrapper pattern, etc.)\n\n### 3. Create `Snowflake/docs/PROCESSES.md` — Operational processes\n\nContent:\n- **Daily Pipeline Schedule** table (UTC + ET times, task, procedure, warehouse)\n- **Task Lifecycle** — states (started, suspended), resume/suspend commands\n- **Monitoring** — how to check execution log, task history, daily email reports\n- **Error Handling** — retry wrapper behavior, TASK_EXECUTION_LOG schema, email alerts on failure\n- **Manual Execution** — how to call each SP manually with examples\n- **Deployment Process** — step-by-step for deploying procedure/task changes (CREATE OR REPLACE in Snowsight)\n- **Account Sync Flow** — how new Salesforce accounts propagate: Data Cloud → datashare → MASTER_ACCOUNTS → TRADE_GENERATION_CONFIG\n\n### 4. Create `Snowflake/docs/ARTIFACTS.md` — Object inventory\n\nContent:\n- **Tables** table (all 41 objects in FINS.PUBLIC — name, rows, owner, purpose, related project)\n- **Stored Procedures** table (user-defined only — 10 procedures with signature, language, purpose, calling task)\n- **Scheduled Tasks** table (8 tasks — name, schedule, warehouse, procedure called, state)\n- **Views** table (3 views — name, purpose)\n- **Notification Integrations** table (2 integrations — name, type, purpose)\n- **Warehouses** table (7 warehouses — name, size, purpose, auto-suspend)\n\n### 5. Create `Snowflake/docs/DIAGRAMS.md` — Consolidated diagram reference\n\nContent:\n- **Monorepo Snowflake Landscape** — Mermaid `graph LR` showing both projects + shared infra\n- **Data Lineage** — Mermaid `flowchart TD` showing Salesforce CRM → Data Cloud → FINSDC3_DATASHARE → FINS.PUBLIC tables → downstream consumers\n- **Daily Execution Timeline** — Mermaid `gantt` chart showing task execution order over 24h\n- **Retry Wrapper Flow** — Mermaid `flowchart TD` with decision diamonds for retry logic\n- **Email Alerting Flow** — Mermaid `sequenceDiagram` showing SP_DAILY_JOB_REPORT → SYSTEM$SEND_EMAIL\n\n### 6. Create `Snowflake/docs/ENVIRONMENT.md` — Snowflake config reference\n\nContent:\n- **Connection Details** table (account, database, schema, role, default warehouse)\n- **Warehouses** table with sizing rationale\n- **External Data Sources** — FINSDC3_DATASHARE details, schema path, caveats\n- **Email Notification Setup** — integration names, recipients\n- **Permissions** — which role owns what, execution context (OWNER vs CALLER)\n\n### 7. Update root-level docs\n\n- Add `Snowflake/` to `JDO/README.md` Projects table\n- Add entry in `JDO/docs/INDEX.md` per-project table\n- Add entry in `JDO/docs/ARTIFACTS.md`\n- Add entry in `JDO/docs/ARCHITECTURE.md` documentation map\n\n## Verification\n\n1. All relative links resolve correctly (test with `grep -r '\\](' Snowflake/ | grep -v 'http'` to find broken links)\n2. Mermaid diagrams render in GitHub preview (validate syntax)\n3. Badge URLs load correctly (shields.io format)\n4. Row counts and object counts in ARTIFACTS.md match live Snowflake state\n5. Cross-references between Snowflake/ docs and the two sibling projects are bidirectional\n\n## Critical Files\n\n- `Snowflake/README.md` — Landing page and hub for all Snowflake documentation\n- `Snowflake/docs/ARCHITECTURE.md` — ER diagrams, data flow, sequence diagrams, design decisions\n- `Snowflake/docs/PROCESSES.md` — Operational runbook: schedules, monitoring, deployment, error handling\n- `Snowflake/docs/ARTIFACTS.md` — Complete inventory of all FINS.PUBLIC objects\n- `JDO/README.md` — Needs update to include Snowflake/ in the Projects table