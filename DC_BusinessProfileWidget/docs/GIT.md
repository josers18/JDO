# Git / monorepo — Business Profile Widget

This project lives in the **JDO** monorepo:

**Path:** `DC_BusinessProfileWidget/`

It is a standalone **Salesforce DX** project (contains **`sfdx-project.json`**). Deploy from this directory; do not assume sibling folders are on the org unless you deploy them separately.

**Related package:** [DC_PersonProfileWidget](../DC_PersonProfileWidget/README.md) (customer profile — Account + Contact, assembly output map + `flow:` / `flows:` / SOQL for slots).

---

[README.md](../README.md)
